name: Reusable Deploy

on:
    workflow_call:
        inputs:
            name:
                description: your name
                required: false
                default: World
                type: string
        secrets:
            some-secret:
                required: false
jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Output 
              run: echo "Hello, ${{ inputs.name }}!"